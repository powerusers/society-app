node_modules
dist
.DS_Store
*.local
.env
.env.*
# The template carries no secrets and is the only record of what the API needs
# in its environment — without this negation `.env.*` silently swallows it.
!.env.example

.shots

# api
apps/api/.env

# Android
apps/android/local.properties
apps/android/.gradle/
apps/android/build/
apps/android/app/build/
apps/android/.idea/
*.keystore
*.jks

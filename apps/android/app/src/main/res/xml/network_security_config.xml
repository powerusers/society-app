<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain HTTP is allowed only to the addresses a development machine uses, so a
  debug build can reach an API running on a laptop. Everything else, including
  every release build, must be HTTPS: resident phone numbers and gate approvals
  are not things to send over a café network in the clear.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
</network-security-config>

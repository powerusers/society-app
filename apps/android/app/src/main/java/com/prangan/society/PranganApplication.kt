package com.prangan.society

import android.app.Application

/**
 * Present so the manifest has something to name and so there is somewhere
 * obvious to put process-wide setup — crash reporting, notification channels —
 * when those arrive.
 */
class PranganApplication : Application()
